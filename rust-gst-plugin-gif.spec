# Generated by rust2rpm 25
# * missing dev-dependency: gstreamer-check ^0.21
%bcond_with check

%global crate gst-plugin-gif

Name:           rust-gst-plugin-gif
Version:        0.11.1
Release:        %autorelease
Summary:        GStreamer GIF plugin

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/gst-plugin-gif
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * downgrade cargo-c dep to the version available in Fedora
Patch:          gst-plugin-gif-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
GStreamer GIF plugin.}

%description %{_description}

%package     -n gstreamer1-plugin-gif
Summary:        %{summary}

# Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        MIT AND (Apache-2.0 OR MIT) AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown

# additionally provide the upstream name
Provides:       %{crate} = %{version}-%{release}

%description -n gstreamer1-plugin-gif %{_description}

%files       -n gstreamer1-plugin-gif
%license LICENSE-APACHE
%license LICENSE-MIT
%license LICENSE.dependencies
%{_libdir}/gstreamer-1.0/libgstgif.so

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+capi-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+capi-devel %{_description}

This package contains library source intended for building other packages which
use the "capi" feature of the "%{crate}" crate.

%files       -n %{name}+capi-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+doc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+doc-devel %{_description}

This package contains library source intended for building other packages which
use the "doc" feature of the "%{crate}" crate.

%files       -n %{name}+doc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+static-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+static-devel %{_description}

This package contains library source intended for building other packages which
use the "static" feature of the "%{crate}" crate.

%files       -n %{name}+static-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'cargo-c'

%build
%cargo_build
%cargo_cbuild
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
%cargo_cinstall
# remove statically linked library
rm -v %{buildroot}/%{_libdir}/gstreamer-1.0/libgstgif.a
# remove unnecessary pkgconfig file
rm -v %{buildroot}/%{_libdir}/pkgconfig/gstgif.pc

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
