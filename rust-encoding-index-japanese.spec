# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate encoding-index-japanese

Name:           rust-encoding-index-japanese
Version:        1.20141219.5
Release:        %autorelease
Summary:        Index tables for Japanese character encodings

# https://github.com/lifthrasiir/rust-encoding/issues/111
License:        CC0-1.0
URL:            https://crates.io/crates/encoding-index-japanese
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * mark dev-dependency as dev-dependency and remove path
Patch:          encoding-index-japanese-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Index tables for Japanese character encodings.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
