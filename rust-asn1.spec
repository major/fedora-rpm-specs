# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate asn1

Name:           rust-asn1
Version:        0.15.5
Release:        %autorelease
Summary:        ASN.1 (DER) parser and writer for Rust

License:        BSD-3-Clause
URL:            https://crates.io/crates/asn1
Source:         %{crates_source}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
ASN.1 (DER) parser and writer for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
# const-generic feature was dropped in asn1 0.14
Obsoletes:      rust-asn1+const-generics-devel < 0.14

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fallible-allocations-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fallible-allocations-devel %{_description}

This package contains library source intended for building other packages which
use the "fallible-allocations" feature of the "%{crate}" crate.

%files       -n %{name}+fallible-allocations-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
