# Generated by rust2rpm 20
%bcond_without check
%global debug_package %{nil}

%global crate vhost

Name:           rust-%{crate}
Version:        0.4.0
Release:        2%{?dist}
Summary:        Pure rust library for vdpa, vhost and vhost-user

# Upstream license specification: Apache-2.0 OR BSD-3-Clause
License:        ASL 2.0 or BSD
URL:            https://crates.io/crates/vhost
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging

%global _description %{expand:
Pure rust library for vdpa, vhost and vhost-user.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE LICENSE-BSD-3-Clause
%doc CHANGELOG.md README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-kern-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-kern-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-kern" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-kern-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-net-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-net-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-net" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-net-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-user-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-user-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-user" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-user-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-user-master-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-user-master-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-user-master" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-user-master-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-user-slave-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-user-slave-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-user-slave" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-user-slave-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-vdpa-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-vdpa-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-vdpa" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-vdpa-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+vhost-vsock-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vhost-vsock-devel %{_description}

This package contains library source intended for building other packages which
use the "vhost-vsock" feature of the "%{crate}" crate.

%files       -n %{name}+vhost-vsock-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Sat Jul 23 2022 Fedora Release Engineering <releng@fedoraproject.org> - 0.4.0-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_37_Mass_Rebuild

* Wed Jun 01 2022 Sergio Lopez <slp@redhat.com> - 0.4.0-1
- Update to 0.4.0

* Thu Feb 17 2022 Sergio Lopez <slp@redhat.com> - 0.3.0-1
- Initial package
