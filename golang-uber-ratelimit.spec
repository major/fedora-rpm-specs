# Generated by go2rpm 1.8.0
%bcond_without check
%global debug_package %{nil}

# https://github.com/uber-go/ratelimit
%global goipath         go.uber.org/ratelimit
%global forgeurl        https://github.com/uber-go/ratelimit
Version:                0.2.0

%gometa

%global common_description %{expand:
A Golang blocking leaky-bucket rate limit implementation.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A Golang blocking leaky-bucket rate limit implementation

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep
# https://github.com/uber-go/ratelimit/commit/bca0419e3ebf1149d54434d8c10ec73b9fc0abf4
sed -i "s|github.com/andres-erbsen/clock|github.com/benbjohnson/clock|" $(find . -name "*.go")

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%ifarch i686 s390x
for test in "TestSlack" "TestDelayedRateLimiter" "TestRateLimiter"\
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
