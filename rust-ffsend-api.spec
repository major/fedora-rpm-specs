# Generated by rust2rpm 20
%bcond_without check
%global debug_package %{nil}

%global crate ffsend-api

Name:           rust-%{crate}
Version:        0.7.3
Release:        %autorelease
Summary:        Fully featured Firefox Send API client

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/ffsend-api
Source:         %{crates_source}
# Initial patched metadata
# * bump version-compare from 0.0.11 to 0.1.0
# * use OpenSSL for crypto by default
# * drop ring dependency and features (not available on all architectures)
Patch0:         ffsend-api-fix-metadata.diff
# * include adapted upstream patch for porting to version-compare 0.1.0:
#   https://github.com/timvisee/ffsend-api/commit/744c8d4
Patch1:         0001-port-to-version-compare-0.1.0.patch

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging

%global _description %{expand:
Fully featured Firefox Send API client.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+crypto-openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+crypto-openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "crypto-openssl" feature of the "%{crate}" crate.

%files       -n %{name}+crypto-openssl-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "openssl" feature of the "%{crate}" crate.

%files       -n %{name}+openssl-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+send2-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+send2-devel %{_description}

This package contains library source intended for building other packages which
use the "send2" feature of the "%{crate}" crate.

%files       -n %{name}+send2-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+send3-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+send3-devel %{_description}

This package contains library source intended for building other packages which
use the "send3" feature of the "%{crate}" crate.

%files       -n %{name}+send3-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+websocket-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+websocket-devel %{_description}

This package contains library source intended for building other packages which
use the "websocket" feature of the "%{crate}" crate.

%files       -n %{name}+websocket-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
