# Generated by rust2rpm 20
%bcond_without check
%global debug_package %{nil}

%global crate os-release

Name:           rust-%{crate}
Version:        0.1.0
Release:        %autorelease
Summary:        Parse /etc/os-release files on Linux distributions

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/os-release
Source:         %{crates_source}
# Initial patched metadata
# Exclude examples
Patch0:         os-release-fix-metadata.diff

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging

%global _description %{expand:
Parse /etc/os-release files on Linux distributions.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
