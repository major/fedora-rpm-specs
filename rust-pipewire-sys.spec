# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate pipewire-sys

Name:           rust-%{crate}
Version:        0.5.0
Release:        %autorelease
Summary:        Rust FFI bindings for PipeWire

# Upstream license specification: MIT
# https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/issues/37
License:        MIT
URL:            https://crates.io/crates/pipewire-sys
Source0:        %{crates_source}
Source1:        https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/raw/v0.4.1/LICENSE

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust FFI bindings for PipeWire.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(libpipewire-0.3) >= 0.3

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'pkgconfig(libpipewire-0.3) >= 0.3'

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip generated doctests that are broken with pipewire 0.3.50+
%cargo_test -- -- --skip bindings::pw_time
%endif

%changelog
%autochangelog
