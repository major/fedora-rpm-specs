# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate pcap

Name:           rust-pcap
Version:        0.7.0
Release:        %autorelease
Summary:        Packet capture API around pcap/wpcap

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pcap
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop optional dependency on the obsolete "clippy" crate
# * add missing required features to the "streamlisten" example binary
Patch:          pcap-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A packet capture API around pcap/wpcap.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       libpcap-devel

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+full-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+full-devel %{_description}

This package contains library source intended for building other packages which
use the "full" feature of the "%{crate}" crate.

%files       -n %{name}+full-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+futures-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+futures-devel %{_description}

This package contains library source intended for building other packages which
use the "futures" feature of the "%{crate}" crate.

%files       -n %{name}+futures-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+mio-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+mio-devel %{_description}

This package contains library source intended for building other packages which
use the "mio" feature of the "%{crate}" crate.

%files       -n %{name}+mio-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+pcap-fopen-offline-precision-devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       libpcap-devel >= 1.5.0

%description -n %{name}+pcap-fopen-offline-precision-devel %{_description}

This package contains library source intended for building other packages which
use the "pcap-fopen-offline-precision" feature of the "%{crate}" crate.

%files       -n %{name}+pcap-fopen-offline-precision-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+pcap-savefile-append-devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       libpcap-devel >= 1.7.2

%description -n %{name}+pcap-savefile-append-devel %{_description}

This package contains library source intended for building other packages which
use the "pcap-savefile-append" feature of the "%{crate}" crate.

%files       -n %{name}+pcap-savefile-append-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio" feature of the "%{crate}" crate.

%files       -n %{name}+tokio-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio-core-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio-core-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio-core" feature of the "%{crate}" crate.

%files       -n %{name}+tokio-core-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'libpcap-devel'

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
