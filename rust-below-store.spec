# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate below-store

Name:           rust-below-store
Version:        0.7.1
Release:        %autorelease
Summary:        Store crate for below

License:        Apache-2.0
URL:            https://crates.io/crates/below-store
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump nix from 0.25 to 0.26
# * bump zstd from 0.11.1 to 0.12
# * bump zstd-safe from 5.0.1 to 6
# * drop version suffix from zstd and zstd-safe deps
# * make zstd a dependency, not a dev-dependency, for no-vendor
# * make no-vendor an explicit no-op feature
Patch:          below-store-fix-metadata.diff
# * port to zstd-safe 6
Patch:          0001-port-to-zstd-safe-6.patch

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Store crate for below.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+no-vendor-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no-vendor-devel %{_description}

This package contains library source intended for building other packages which
use the "no-vendor" feature of the "%{crate}" crate.

%files       -n %{name}+no-vendor-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
