# Generated by rust2rpm 25
%bcond_without check
%global debug_package %{nil}

%global crate argfile

Name:           rust-argfile
Version:        0.1.6
Release:        %autorelease
Summary:        Load additional CLI args from file

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/argfile
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Load additional CLI args from file.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+response-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+response-devel %{_description}

This package contains library source intended for building other packages which
use the "response" feature of the "%{crate}" crate.

%files       -n %{name}+response-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+shlex-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+shlex-devel %{_description}

This package contains library source intended for building other packages which
use the "shlex" feature of the "%{crate}" crate.

%files       -n %{name}+shlex-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
