# Generated by rust2rpm 22
# * tracing-log and tracing-subscriber are not packaged yet
%bcond_with check
%global debug_package %{nil}

%global crate warp

Name:           rust-warp
Version:        0.3.2
Release:        %autorelease
Summary:        Serve the web at warp speeds

License:        MIT
URL:            https://crates.io/crates/warp
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump tokio-tungstenite from 0.15 to 0.17:
#   https://github.com/seanmonstar/warp/commit/11169f2
# * drop unused multipart feature with outdated dependencies
Patch:          warp-fix-metadata.diff
Patch:          0001-port-to-tungstenite-0.17.patch

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Serve the web at warp speeds.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+async-compression-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+async-compression-devel %{_description}

This package contains library source intended for building other packages which
use the "async-compression" feature of the "%{crate}" crate.

%files       -n %{name}+async-compression-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+compression-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+compression-devel %{_description}

This package contains library source intended for building other packages which
use the "compression" feature of the "%{crate}" crate.

%files       -n %{name}+compression-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+compression-brotli-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+compression-brotli-devel %{_description}

This package contains library source intended for building other packages which
use the "compression-brotli" feature of the "%{crate}" crate.

%files       -n %{name}+compression-brotli-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+compression-gzip-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+compression-gzip-devel %{_description}

This package contains library source intended for building other packages which
use the "compression-gzip" feature of the "%{crate}" crate.

%files       -n %{name}+compression-gzip-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio-tungstenite-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio-tungstenite-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio-tungstenite" feature of the "%{crate}" crate.

%files       -n %{name}+tokio-tungstenite-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+websocket-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+websocket-devel %{_description}

This package contains library source intended for building other packages which
use the "websocket" feature of the "%{crate}" crate.

%files       -n %{name}+websocket-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
