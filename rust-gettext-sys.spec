# Generated by rust2rpm 18
%bcond_without check
%global debug_package %{nil}

%global crate gettext-sys

Name:           rust-%{crate}
Version:        0.21.2
Release:        %autorelease
Summary:        Raw FFI bindings for gettext

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/gettext-sys
Source:         %{crates_source}
# Initial patched metadata
# * drop unnecessary build-dependencies
Patch0:         gettext-sys-fix-metadata.diff
# always build against system gettext, whether feature is enabled or not
Patch1:         0001-always-build-against-system-gettext.patch

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Raw FFI bindings for gettext.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE.txt
%doc README.md CHANGELOG.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+gettext-system-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gettext-system-devel %{_description}

This package contains library source intended for building other packages
which use "gettext-system" feature of "%{crate}" crate.

%files       -n %{name}+gettext-system-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
rm -v gettext-*.tar.*
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
