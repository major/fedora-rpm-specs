# Generated by rust2rpm 18
%bcond_without check
%global debug_package %{nil}

%global crate thread-tree

Name:           rust-%{crate}
Version:        0.3.2
Release:        %autorelease
Summary:        Tree-structured thread pool for splitting jobs hierarchically

# Upstream license specification: MIT OR Apache-2.0
License:        MIT or ASL 2.0
URL:            https://crates.io/crates/thread-tree
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Tree-structured thread pool for splitting jobs hierarchically on worker
threads.
The tree structure means that there is no contention between workers when
delivering jobs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE-MIT LICENSE-APACHE
%doc RELEASES.md README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+unstable-thread-sea-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-thread-sea-devel %{_description}

This package contains library source intended for building other packages
which use "unstable-thread-sea" feature of "%{crate}" crate.

%files       -n %{name}+unstable-thread-sea-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
