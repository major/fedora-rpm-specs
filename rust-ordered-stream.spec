# Generated by rust2rpm 22
%bcond_without check
%global debug_package %{nil}

%global crate ordered-stream

Name:           rust-ordered-stream
Version:        0.0.1
Release:        %autorelease
Summary:        Streams that are ordered relative to external events

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/ordered-stream
Source0:        %{crates_source}
# https://github.com/danieldg/ordered-stream/issues/1
# https://github.com/danieldg/ordered-stream/pull/2
Source1:        https://github.com/danieldg/ordered-stream/raw/25cc74c/LICENSE-APACHE
Source2:        https://github.com/danieldg/ordered-stream/raw/25cc74c/LICENSE-MIT

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Streams that are ordered relative to external events.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -pav %{SOURCE1} %{SOURCE2} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
