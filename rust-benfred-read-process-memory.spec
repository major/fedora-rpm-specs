# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate benfred-read-process-memory

Name:           rust-benfred-read-process-memory
Version:        0.2.1
Release:        %autorelease
Summary:        Read memory from another process

License:        MIT
URL:            https://crates.io/crates/benfred-read-process-memory
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          benfred-read-process-memory-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * prevent test binary from being built and installed
Patch:          benfred-read-process-memory-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Read memory from another process.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/License.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip tests that require running the dropped test binary
%cargo_test -- -- --skip test::test_read_large --skip test::test_read_small
%endif

%changelog
%autochangelog
