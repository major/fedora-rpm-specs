# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate tree-sitter-tags

Name:           rust-%{crate}
Version:        0.20.2
Release:        %autorelease
Summary:        Library for extracting tag information

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/tree-sitter-tags
Source:         %{crates_source}
# Upstream license file - tree-sitter/tree-sitter#1520
Source1:        https://github.com/tree-sitter/tree-sitter/raw/v%{version}/LICENSE#/LICENSE.upstream
# Fix tree-sitter-cli tests on non-x86 architectures
# Part of tree-sitter/tree-sitter#1524
Patch0:         tags-use-platform-independent-c_char-instead-of-i8.patch

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Library for extracting tag information.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p2
%cargo_prep
cp %{SOURCE1} LICENSE

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
