# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate versionize

Name:           rust-versionize
Version:        0.1.10
Release:        %autorelease
Summary:        Version tolerant serialization/deserialization framework

License:        Apache-2.0
URL:            https://crates.io/crates/versionize
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * exclude maintainer-only files from the package
# * upgrade crc64 to latest (backport 97d83ee205064b6dc75f1a7e03b4bbe4d05e8488)
Patch:          versionize-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A version tolerant serialization/deserialization framework.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/SECURITY-POLICY.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch %{ix86}
# * skip corrupt value tests on 32-bit targets (for using 64-bit usize values):
%cargo_test -- -- --exact --skip=primitives::tests::test_corrupted_{string,vec}_len
%else
%cargo_test
%endif
%endif

%changelog
%autochangelog
