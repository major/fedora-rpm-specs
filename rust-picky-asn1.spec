# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate picky-asn1

Name:           rust-picky-asn1
Version:        0.5.0
Release:        %autorelease
Summary:        Provide ASN.1 simple types

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/picky-asn1
Source:         %{crates_source}
# https://github.com/Devolutions/picky-rs/issues/230
Source:         https://github.com/Devolutions/picky-rs/raw/7ddc2e4/LICENSE-APACHE
Source:         https://github.com/Devolutions/picky-rs/raw/7ddc2e4/LICENSE-MIT

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Provide ASN.1 simple types.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono" feature of the "%{crate}" crate.

%files       -n %{name}+chrono-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono_conversion-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono_conversion-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono_conversion" feature of the "%{crate}" crate.

%files       -n %{name}+chrono_conversion-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+time-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+time-devel %{_description}

This package contains library source intended for building other packages which
use the "time" feature of the "%{crate}" crate.

%files       -n %{name}+time-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+time_conversion-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+time_conversion-devel %{_description}

This package contains library source intended for building other packages which
use the "time_conversion" feature of the "%{crate}" crate.

%files       -n %{name}+time_conversion-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * doctests can only be run in-tree
%cargo_test -- --lib
%endif

%changelog
%autochangelog
