# Generated by rust2rpm 25
%bcond_without check

%global crate uu_shuf

Name:           rust-uu_shuf
Version:        0.0.23
Release:        %autorelease
Summary:        shuf ~ (uutils) display random permutations of input lines

# * uu_shuf except src/rand_read_adapter.rs: MIT
# * src/rand_read_adapter.rs: MIT OR Apache-2.0, distributing as MIT
#   since the Apache license text is not provided
License:        MIT
URL:            https://crates.io/crates/uu_shuf
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
shuf ~ (uutils) display random permutations of input lines.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        MIT AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc BENCHMARKING.md
%doc shuf.md
%{_bindir}/%{crate}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/BENCHMARKING.md
%doc %{crate_instdir}/shuf.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
mv %{buildroot}%{_bindir}/shuf %{buildroot}%{_bindir}/%{crate}

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
