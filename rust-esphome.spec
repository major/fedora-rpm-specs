# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate esphome

Name:           rust-%{crate}
Version:        0.1.0
Release:        %autorelease
Summary:        ESPHome API client for Rust

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/esphome
Source:         %{crates_source}
# Initial patched metadata
# Bump num-derive to 0.3: https://github.com/pixelspark/esphome-rs/pull/1
Patch0:         esphome-fix-metadata.diff

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
ESPHome API client for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
