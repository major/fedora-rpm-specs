# Generated by rust2rpm 18
%bcond_without check
%global debug_package %{nil}

%global crate protobuf-codegen-pure

Name:           rust-%{crate}
Version:        2.25.2
Release:        %autorelease
Summary:        Pure-rust codegen for protobuf using protobuf-parser crate

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/protobuf-codegen-pure
Source:         %{crates_source}
# Initial patched metadata
# * drop unused parse-and-typecheck executable
Patch0:         protobuf-codegen-pure-fix-metadata.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Pure-rust codegen for protobuf using protobuf-parser crate
WIP.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE.txt
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# files needed for integration tests are not shipped with published crates
%cargo_test -- --lib
%endif

%changelog
%autochangelog
