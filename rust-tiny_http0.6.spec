# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate tiny_http

Name:           rust-tiny_http0.6
Version:        0.6.4
Release:        %autorelease
Summary:        Low level HTTP server library

License:        Apache-2.0
URL:            https://crates.io/crates/tiny_http
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump ascii from 0.8 to 1:
#   https://github.com/tiny-http/tiny-http/commit/fca82d3
# * bump chunked_transfer from 0.3 to 1:
#   https://github.com/tiny-http/tiny-http/commit/a3013cf
Patch:          tiny_http-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Low level HTTP server library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "openssl" feature of the "%{crate}" crate.

%files       -n %{name}+openssl-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ssl-devel %{_description}

This package contains library source intended for building other packages which
use the "ssl" feature of the "%{crate}" crate.

%files       -n %{name}+ssl-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
