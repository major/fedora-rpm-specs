# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate picky-asn1-x509

Name:           rust-picky-asn1-x509
Version:        0.7.0
Release:        %autorelease
Summary:        ASN1 types defined by X.509 related RFCs

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/picky-asn1-x509
Source:         %{crates_source}
# https://github.com/Devolutions/picky-rs/issues/230
Source:         https://github.com/Devolutions/picky-rs/raw/5f27522/LICENSE-APACHE
Source:         https://github.com/Devolutions/picky-rs/raw/5f27522/LICENSE-MIT
# Manually created patch for downstream crate metadata changes
# * drop unused features with missing dependencies
Patch:          picky-asn1-x509-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Provides ASN1 types defined by X.509 related RFCs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+legacy-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+legacy-devel %{_description}

This package contains library source intended for building other packages which
use the "legacy" feature of the "%{crate}" crate.

%files       -n %{name}+legacy-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+num-bigint-dig-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+num-bigint-dig-devel %{_description}

This package contains library source intended for building other packages which
use the "num-bigint-dig" feature of the "%{crate}" crate.

%files       -n %{name}+num-bigint-dig-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
