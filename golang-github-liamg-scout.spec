# Generated by go2rpm 1
%bcond_without check

# https://github.com/liamg/scout
%global goipath         github.com/liamg/scout
Version:                0.15.2

%gometa

%global common_description %{expand:
A lightweight URL fuzzer and spider: Discover a web server's undisclosed files,
directories and VHOSTs.}

%global golicenses      LICENSE
%global godocs          README.md assets/wordlist.txt assets/vhost.txt

Name:           %{goname}
Release:        %autorelease
Summary:        Lightweight URL fuzzer and spider

License:        Unlicense
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/avast/retry-go)
BuildRequires:  golang(github.com/liamg/tml)
BuildRequires:  golang(github.com/sirupsen/logrus)
BuildRequires:  golang(github.com/spf13/cobra)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
BuildRequires:  golang(github.com/stretchr/testify/require)
%endif

%description
%{common_description}

%package -n     scout
Summary:        %{summary}

%description -n scout
%{summary}.

%gopkg

%prep
%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files -n scout
%license LICENSE
%doc README.md assets/wordlist.txt assets/vhost.txt
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog

