# Generated by rust2rpm 24
# * missing dev-dependencies: futures-intrusive, smol
%bcond_with check
%global debug_package %{nil}

%global crate async-mutex

Name:           rust-async-mutex
Version:        1.4.0
Release:        %autorelease
Summary:        Async mutex

# Llicense files are missing from published crates, but the project has been
# merged into async-lock crate and is no longer developed separately.
License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/async-mutex
Source:         %{crates_source}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Async mutex.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
