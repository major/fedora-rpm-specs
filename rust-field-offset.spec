# Generated by rust2rpm 17
%bcond_without check
%global debug_package %{nil}

%global crate field-offset

Name:           rust-%{crate}
Version:        0.3.4
Release:        %autorelease
Summary:        Safe pointer-to-member implementation

# Upstream license specification: MIT OR Apache-2.0
License:        MIT or ASL 2.0
URL:            https://crates.io/crates/field-offset
Source:         %{crates_source}
# Download License files
# https://github.com/Diggsey/rust-field-offset/pull/18
Source1:        https://raw.githubusercontent.com/Diggsey/rust-field-offset/master/LICENSE-APACHE
Source2:        https://raw.githubusercontent.com/Diggsey/rust-field-offset/master/LICENSE-MIT

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Safe pointer-to-member implementation.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE-MIT LICENSE-APACHE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp %{SOURCE1} LICENSE-APACHE
cp %{SOURCE2} LICENSE-MIT

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
