# Generated by rust2rpm 23
%bcond_without check
%global debug_package %{nil}

%global crate below-gpu-stats

Name:           rust-below-gpu-stats
Version:        0.6.3
Release:        %autorelease
Summary:        GPU stats crate for below

License:        Apache-2.0
URL:            https://crates.io/crates/below-gpu-stats
Source:         %{crates_source}
# LICENSE file not added to this crate yet:
# https://github.com/facebookincubator/below/pull/8176
Source1:        https://raw.githubusercontent.com/facebookincubator/below/main/LICENSE

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
GPU stats crate for below.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -p %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
