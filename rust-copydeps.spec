# Generated by rust2rpm 21
%bcond_without check

%global crate copydeps

Name:           rust-%{crate}
Version:        5.0.1
Release:        %autorelease
Summary:        Find and copy all the .so / .dll files needed by an executable

# Upstream license specification: GPL-3.0-or-later
License:        GPLv3+
URL:            https://crates.io/crates/copydeps
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Find and copy all the .so / .dll files needed by an executable.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# GPLv3+
# MIT
# MIT or ASL 2.0
# Unlicense or MIT
License:        GPLv3+ and MIT

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENCE.txt
%doc README.md
%{_bindir}/copydeps
%{_mandir}/man1/%{crate}.1*
%dir %{_datadir}/bash-completion
%dir %{_datadir}/bash-completion/completions
%{_datadir}/bash-completion/completions/%{crate}

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install
install -m 644 -Dp misc/%{crate}.man %{buildroot}%{_mandir}/man1/%{crate}.1
install -m 644 -Dp misc/%{crate}.bash-completion %{buildroot}%{_datadir}/bash-completion/completions/%{crate}

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
