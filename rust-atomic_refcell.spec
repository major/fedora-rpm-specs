# Generated by rust2rpm 23
%bcond_without check
%global debug_package %{nil}

%global crate atomic_refcell

Name:           rust-atomic_refcell
Version:        0.1.8
Release:        %autorelease
Summary:        Threadsafe RefCell

# Upstream license specification: Apache-2.0/MIT
License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/atomic_refcell
Source:         %{crates_source}
# License added, not yet released
# https://github.com/bholley/atomic_refcell/issues/17
Source:         https://raw.githubusercontent.com/bholley/%{crate}/3ae6dab/LICENSE.MIT
Source:         https://raw.githubusercontent.com/bholley/%{crate}/8995cb9/LICENSE.APACHE

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Threadsafe RefCell.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE.MIT
%license LICENSE.APACHE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp %{SOURCE1} .
cp %{SOURCE2} .
%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
