# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate libspa

Name:           rust-%{crate}
Version:        0.5.0
Release:        %autorelease
Summary:        Rust bindings for libspa

# Upstream license specification: MIT
# https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/issues/37
License:        MIT
URL:            https://crates.io/crates/libspa
Source0:        %{crates_source}
Source1:        https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/raw/v%{version}/LICENSE

# * work around architecture-specific test failures:
#   https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/issues/35
Patch:          https://gitlab.freedesktop.org/pipewire/pipewire-rs/-/merge_requests/122.patch#/libspa-use_platform_indep_c_char.patch

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust bindings for libspa.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p2
cp %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch %{arm} %{ix86}
%cargo_test -- -- --skip 'result::tests::async_seq_panic' --skip 'pointer'
%else
%cargo_test -- -- --skip 'result::tests::async_seq_panic'
%endif
%endif

%changelog
%autochangelog
