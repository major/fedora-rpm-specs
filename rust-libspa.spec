# Generated by rust2rpm 24
# * resolve circular dependency with pipewire and pipewire-sys
%bcond_with check
%global debug_package %{nil}

%global crate libspa

Name:           rust-libspa
Version:        0.6.0
Release:        %autorelease
Summary:        Rust bindings for libspa

License:        MIT
URL:            https://crates.io/crates/libspa
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * fix libspa-sys, pipewire, pipewire-sys dependency restrictions
Patch:          libspa-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust bindings for libspa.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+v0_3_33-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+v0_3_33-devel %{_description}

This package contains library source intended for building other packages which
use the "v0_3_33" feature of the "%{crate}" crate.

%files       -n %{name}+v0_3_33-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
