# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate systemstat

Name:           rust-systemstat
Version:        0.2.3
Release:        %autorelease
Summary:        Get system information/statistics in a cross-platform way

License:        Unlicense
URL:            https://crates.io/crates/systemstat
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          systemstat-fix-metadata-auto.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Get system information/statistics in a cross-platform way.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/UNLICENSE
%doc %{crate_instdir}/CODE_OF_CONDUCT.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+the_serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+the_serde-devel %{_description}

This package contains library source intended for building other packages which
use the "the_serde" feature of the "%{crate}" crate.

%files       -n %{name}+the_serde-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch %{ix86}
# thread 'platform::linux::test_proc_meminfo' panicked at 'assertion failed: `(left == right)`
#  left: `46`,
#  right: `47`'
%cargo_test -- -- --skip platform::linux::test_proc_meminfo
%else
%cargo_test
%endif
%endif

%changelog
%autochangelog
