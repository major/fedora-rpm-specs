# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate gptman

Name:           rust-gptman
Version:        1.0.2
Release:        %autorelease
Summary:        GPT manager that allows you to copy partitions from one disk to another

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/gptman
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * disable CLI binary and remove CLI-only optional dependencies
Patch:          gptman-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A GPT manager that allows you to copy partitions from one disk to
another.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.Apache-2.0
%license %{crate_instdir}/LICENSE.MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nix-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nix-devel %{_description}

This package contains library source intended for building other packages which
use the "nix" feature of the "%{crate}" crate.

%files       -n %{name}+nix-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
