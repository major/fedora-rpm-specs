# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate cryptoki

Name:           rust-%{crate}
Version:        0.3.0
Release:        2%{?dist}
Summary:        Rust-native wrapper around the PKCS #11 API

License:        Apache-2.0
URL:            https://crates.io/crates/cryptoki
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust-native wrapper around the PKCS #11 API.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+generate-bindings-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+generate-bindings-devel %{_description}

This package contains library source intended for building other packages which
use the "generate-bindings" feature of the "%{crate}" crate.

%files       -n %{name}+generate-bindings-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+psa-crypto-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+psa-crypto-devel %{_description}

This package contains library source intended for building other packages which
use the "psa-crypto" feature of the "%{crate}" crate.

%files       -n %{name}+psa-crypto-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+psa-crypto-conversions-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+psa-crypto-conversions-devel %{_description}

This package contains library source intended for building other packages which
use the "psa-crypto-conversions" feature of the "%{crate}" crate.

%files       -n %{name}+psa-crypto-conversions-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -a
echo "softhsm"

%build
%cargo_build -a

%install
%cargo_install -a

%if %{with check}
%check
export PKCS11_SOFTHSM2_MODULE=%{_libdir}/pkcs11/libsofthsm2.so
mkdir /tmp/tokens
echo "directories.tokendir = /tmp/tokens" > /tmp/softhsm2.conf
export SOFTHSM2_CONF=/tmp/softhsm2.conf
%cargo_test -a
%endif

%changelog
* Fri Jan 20 2023 Fedora Release Engineering <releng@fedoraproject.org> - 0.3.0-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_38_Mass_Rebuild

%autochangelog
