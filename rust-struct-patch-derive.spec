# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate struct-patch-derive

Name:           rust-struct-patch-derive
Version:        0.2.3
Release:        %autorelease
Summary:        Library that helps you implement partial updates for your structs

License:        MIT
URL:            https://crates.io/crates/struct-patch-derive
Source0:        %{crates_source}
# https://github.com/yanganto/struct-patch/pull/17
Source1:        https://github.com/yanganto/struct-patch/raw/859a473/LICENSE

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A library that helps you implement partial updates for your structs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+status-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+status-devel %{_description}

This package contains library source intended for building other packages which
use the "status" feature of the "%{crate}" crate.

%files       -n %{name}+status-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
