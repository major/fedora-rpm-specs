# Generated by go2rpm 1.6.0
%bcond_without check

# https://github.com/pelletier/go-toml
%global goipath         github.com/pelletier/go-toml/v2
Version:                2.0.0~beta.8
%global tag             %(echo v%{version} | sed -e 's/~/-/')

%gometa

%global common_description %{expand:
Go library for the TOML file format.}

%global golicenses      LICENSE
%global godocs          SECURITY.md CONTRIBUTING.md README.md

Name:           %{goname}
Release:        %{autorelease}
Summary:        Go library for the TOML file format

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
# https://github.com/pelletier/go-toml/issues/760
Patch:         go-toml-2.0.0-beta.8-fix-test.patch

%description
%{common_description}

%gopkg

%prep
%goprep
%patch -p1

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/{jsontoml,tomljson,tomll} ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc SECURITY.md CONTRIBUTING.md README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
