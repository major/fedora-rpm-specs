# Generated by go2rpm 1.8.2
%bcond_without check
%global debug_package %{nil}

# https://github.com/fsnotify/fsnotify
%global goipath         github.com/fsnotify/fsnotify
Version:                1.6.0

%gometa

%global goaltipaths     gopkg.in/fsnotify/fsnotify.v1 gopkg.in/fsnotify/v1/fsnotify

%global common_description %{expand:
Cross-platform file system notifications for Go.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Cross-platform file system notifications for Go

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
# https://github.com/fsnotify/fsnotify/issues/538
for test in "TestInotifyDeleteOpenFile" "TestInotifyNoBlockingSyscalls" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
