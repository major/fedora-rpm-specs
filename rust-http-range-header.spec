# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate http-range-header

Name:           rust-http-range-header
Version:        0.3.0
Release:        %autorelease
Summary:        No-dep range header parser

License:        MIT
URL:            https://crates.io/crates/http-range-header
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop unused, benchmark-only criterion dev-dependency to speed up builds
Patch:          http-range-header-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
No-dep range header parser.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/Changelog.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+with_error_cause-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+with_error_cause-devel %{_description}

This package contains library source intended for building other packages which
use the "with_error_cause" feature of the "%{crate}" crate.

%files       -n %{name}+with_error_cause-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
