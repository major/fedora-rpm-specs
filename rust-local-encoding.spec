# Generated by rust2rpm 21
%bcond_without check
%global debug_package %{nil}

%global crate local-encoding

Name:           rust-%{crate}
Version:        0.2.0
Release:        %autorelease
Summary:        Rust library for encoding/decoding a string with local charset

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/local-encoding
Source:         %{crates_source}
# Initial patched metadata
# - Bump skeptic to 0.13: https://github.com/bozaro/local-encoding-rs/pull/4
# - Drop Windows dependencies
Patch0:         local-encoding-fix-metadata.diff

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust library for encoding/decoding a string with local charset. It is useful
for working with ANSI strings on Windows.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
