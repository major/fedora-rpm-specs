# Generated by rust2rpm 20
%bcond_without check
%global debug_package %{nil}

%global crate sequoia-openpgp-mt

Name:           rust-%{crate}
Version:        0.1.0
Release:        %autorelease
Summary:        Multi-threaded parsing of OpenPGP data

# Upstream license specification: LGPL-2.0-or-later
License:        LGPLv2+
URL:            https://crates.io/crates/sequoia-openpgp-mt
Source:         %{crates_source}
# Initial patched metadata
# * set default sequoia-openpgp crypto backend to nettle
# * drop Windows-specific dependencies
Patch0:         sequoia-openpgp-mt-fix-metadata.diff

# * patch to fix doctests with Rust 1.62+
Patch1:         0001-ignore-doctests-for-non-exported-macro_rules-macros.patch

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging

%global _description %{expand:
Multi-threaded parsing of OpenPGP data.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE.txt
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
