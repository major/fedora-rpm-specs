# Generated by rust2rpm 24
# * resolve circular dependency with nalgebra
%bcond_with check
%global debug_package %{nil}

%global crate nalgebra-macros

Name:           rust-nalgebra-macros
Version:        0.2.1
Release:        %autorelease
Summary:        Procedural macros for nalgebra

License:        Apache-2.0
URL:            https://crates.io/crates/nalgebra-macros
# https://github.com/dimforge/nalgebra/issues/912
Source0:        %{crates_source}
Source1:        https://github.com/dimforge/nalgebra/raw/e977ad8/LICENSE

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Procedural macros for nalgebra.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
