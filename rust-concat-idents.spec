# Generated by rust2rpm 22
# * test fixtures are outdated and don't match error output from recent rustc
%bcond_with check
%global debug_package %{nil}

%global crate concat-idents

Name:           rust-concat-idents
Version:        1.1.3
Release:        %autorelease
Summary:        Allows concatenating multiple identifiers and using them everywhere

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/concat-idents
Source:         %{crates_source}
# https://github.com/DzenanJupic/concat-idents/pull/1
Source1:        https://github.com/DzenanJupic/concat-idents/raw/7adc087/APACHE-LICENCE#/LICENSE-APACHE
Source2:        https://github.com/DzenanJupic/concat-idents/raw/7adc087/MIT-LICENSE#/LICENSE-MIT

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Allows concatenating multiple identifiers and using them everywhere.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE-APACHE
%license LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -pav %{SOURCE1} .
cp -pav %{SOURCE2} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
