# Generated by rust2rpm 20
%bcond_without check
%global debug_package %{nil}

%global crate crossfont

Name:           rust-%{crate}
Version:        0.3.2
Release:        %autorelease
Summary:        Cross platform native font loading and rasterization

# Upstream license specification: Apache-2.0
License:        ASL 2.0
URL:            https://crates.io/crates/crossfont
Source:         %{crates_source}
# Initial patched metadata
# * drop windows- and mac OS-specific dependencies
Patch0:         crossfont-fix-metadata.diff

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging

%global _description %{expand:
Cross platform native font loading and rasterization.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc CHANGELOG.md README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/
%exclude %{cargo_registry}/%{crate}-%{version_no_tilde}/rustfmt.toml

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+force_system_fontconfig-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+force_system_fontconfig-devel %{_description}

This package contains library source intended for building other packages which
use the "force_system_fontconfig" feature of the "%{crate}" crate.

%files       -n %{name}+force_system_fontconfig-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
