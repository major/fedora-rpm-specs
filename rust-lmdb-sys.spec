# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate lmdb-sys

Name:           rust-lmdb-sys
Version:        0.8.0
Release:        %autorelease
Summary:        Rust bindings for liblmdb

# https://github.com/danburkert/lmdb-rs/pull/54
License:        Apache-2.0
URL:            https://crates.io/crates/lmdb-sys
Source:         %{crates_source}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust bindings for liblmdb.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(lmdb)

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
# remove bundled copy of lmdb
rm -rv lmdb/
# fix pkg-config name in build.rs
sed -i -e "/pkg_config/s/liblmdb/lmdb/" build.rs
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'pkgconfig(lmdb)'

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
