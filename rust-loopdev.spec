# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate loopdev

Name:           rust-loopdev
Version:        0.4.0
Release:        %autorelease
Summary:        Setup and control loop devices

License:        MIT
URL:            https://crates.io/crates/loopdev
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump bindgen build-dependency from 0.59 to 0.63
Patch:          loopdev-fix-metadata.diff
Patch:          loopdev-fix-test.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Setup and control loop devices.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+direct_io-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+direct_io-devel %{_description}

This package contains library source intended for building other packages which
use the "direct_io" feature of the "%{crate}" crate.

%files       -n %{name}+direct_io-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test -- -- bindings
%endif

%changelog
%autochangelog
