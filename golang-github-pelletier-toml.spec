# Generated by go2rpm
%bcond_without check

# https://github.com/pelletier/go-toml
%global goipath         github.com/pelletier/go-toml
Version:                1.9.4

%gometa

%global common_description %{expand:
Go-toml provides the following features for using data parsed from TOML
documents:

 - Load TOML documents from files and string data
 - Easily navigate TOML structure using Tree
 - Mashaling and unmarshaling to and from data structures
 - Line & column position data for all parsed elements
 - Query support similar to JSON-Path
 - Syntax errors contain line and column numbers}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

%global gosupfiles glide.lock glide.yaml

Name:           %{goname}
Release:        %autorelease
Summary:        Go library for the toml language

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

%description
%{common_description}

%gopkg

%prep
%goprep
cp %{S:1} %{S:2} .

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/jsontoml cmd/tomljson cmd/tomll; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                                             %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/{jsontoml,tomljson,tomll} %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/{jsontoml,tomljson,tomll}

%gopkgfiles

%changelog
%autochangelog
