# Generated by rust2rpm 23
%bcond_without check
%global debug_package %{nil}

%global crate rust-embed

Name:           rust-rust-embed
Version:        6.4.2
Release:        %autorelease
Summary:        Custom Derive Macro which loads files into the rust binary

License:        MIT
URL:            https://crates.io/crates/rust-embed
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Remove unused optional features:
#   - actix support: unused
#   - axum support: not packaged yet
#   - compression support: include-flate not packaged
#   - poem support: not packaged yet
#   - rocket support: unused
#   - salvo support: unused
#   - warp support: unused
Patch:          rust-embed-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Rust Custom Derive Macro which loads files into the rust binary at compile time
during release and loads the file from the fs during dev.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/license
%doc %{crate_instdir}/changelog.md
%doc %{crate_instdir}/readme.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+debug-embed-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+debug-embed-devel %{_description}

This package contains library source intended for building other packages which
use the "debug-embed" feature of the "%{crate}" crate.

%files       -n %{name}+debug-embed-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+include-exclude-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+include-exclude-devel %{_description}

This package contains library source intended for building other packages which
use the "include-exclude" feature of the "%{crate}" crate.

%files       -n %{name}+include-exclude-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+interpolate-folder-path-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+interpolate-folder-path-devel %{_description}

This package contains library source intended for building other packages which
use the "interpolate-folder-path" feature of the "%{crate}" crate.

%files       -n %{name}+interpolate-folder-path-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# doctests are broken
%cargo_test -- --lib
%endif

%changelog
%autochangelog
